# Candidate ONLY for a separate Python 3.12 CPU environment, never the live runtime.
# Not a fully hash-locked transitive wheel inventory or proof of a real-model pilot.
# First install torch==2.8.0 and torchaudio==2.8.0 from the official CPU-only index:
# https://download.pytorch.org/whl/cpu
# https://pytorch.org/get-started/previous-versions/#v280
# Then resolve these pins in that isolated environment and seal all wheel hashes.
# Repo architecture baseline: docs/adr/0007-speaker-and-active-speaker-models.md
# SpeechBrain implementation reference: https://github.com/speechbrain/speechbrain/tree/v1.0.3
# ORT release: https://pypi.org/project/onnxruntime/1.22.1/
# NumPy release: https://pypi.org/project/numpy/2.2.6/
torch==2.8.0+cpu
torchaudio==2.8.0+cpu
speechbrain==1.0.3
onnxruntime==1.22.1
numpy==2.2.6
