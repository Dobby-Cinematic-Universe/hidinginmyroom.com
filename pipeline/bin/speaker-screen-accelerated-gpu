#!/usr/bin/env sh
set -eu
if [ "$#" -lt 1 ]; then
    echo "Usage: speaker-screen-accelerated-gpu /absolute/isolated/python {plan|run|status} ..." >&2
    exit 2
fi
SCREEN_PYTHON=$1
shift
case "$SCREEN_PYTHON" in
    /*) ;;
    *) echo "An absolute isolated Python path is required." >&2; exit 2 ;;
esac
SCRIPT_DIR=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
exec systemd-run --user --scope --quiet \
    --property=MemoryMax=4G --property=MemorySwapMax=0 \
    -- "$SCREEN_PYTHON" -B "$SCRIPT_DIR/../speaker_screen_accelerated.py" "$@"
