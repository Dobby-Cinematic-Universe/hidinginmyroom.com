# Put these assignments in the repository's PRIVATE .env, mode 0600.
# Never source it as a shell script or include credentials in launch arguments.
ASSEMBLYAI_API_KEY=
REVAI_ACCESS_TOKEN=
GEMINI_API_KEY=
# REVAI_API_KEY is accepted as an alias. Do not set conflicting aliases.
